
#  #DockerCompose (http://config-service:8001), without Docker (http://localhost:8001)
#  #User and Password in config-service (application.properties)

spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:8001
      fail-fast: true
      username: user
      password: admin